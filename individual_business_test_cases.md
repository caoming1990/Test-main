# 个体工商户业务测试用例

## 测试范围

- 正常登录后切换进入 `个体工商户结算系统`
- 商户财务管理下 6 个子菜单的查询功能校验
- 基于页面实际元素和按钮生成详细查询用例

## 前置条件

- 测试环境地址：`http://192.168.12.36:7050/login`
- 测试手机号：`13600000000`
- 测试验证码：`0000`
- 登录成功后切换到 `个体工商户结算系统` ，再进入 `商户财务管理`
- 本文档基于 2026-04-02 实际页面走查结果整理

## 页面实际查询元素

| 菜单 | 查询项 | 按钮 |
| --- | --- | --- |
| 充值审核 | 申请时间（开始日期 ~ 结束日期）；所属机构；主体；查询信息（输入订单号查询）；收费类型；模式；是否合并账户；充值角色；充值类型；个体户名称（请输入个体户名称） | 查询、刷新、企业支付宝充值、连连网银充值、导出记录 |
| 充值审核记录 | 申请时间（开始日期 ~ 结束日期）；所属机构；主体；支付渠道；查询信息（输入订单号查询）；充值状态；审核人；账户类型；业务模式；收费类型；是否合并账户；充值角色；充值类型；个体户名称（请输入个体户名称） | 查询、刷新、导出记录 |
| 商户入金记录 | 交易时间（开始日期 ~ 结束日期）；所属机构；主体；支付渠道；平台订单号（请输入平台订单号）；交易流水号（请输入交易流水号）；模式；审核人；账户类型；收费类型；是否合并账户 | 查询、刷新、导出记录 |
| 商户出金记录 | 交易时间（开始日期 ~ 结束日期）；所属机构；主体；支付渠道；平台订单号（请输入平台订单号）；交易流水号（请输入交易流水号）；账户类型；交易类型；收费类型；是否合并账户 | 查询、刷新、导出记录 |
| 提现审核 | 申请时间（开始日期 ~ 结束日期）；所属机构；主体；订单号（请输入订单号）；模式；收费类型；是否合并账户；提现角色；个体户法人信息（请输入法人姓名、身份证） | 查询、刷新、导出记录 |
| 提现记录 | 申请时间（开始日期 ~ 结束日期）；所属机构；主体；订单号（请输入订单号）；模式；收费类型；是否合并账户；提现角色；个体户法人信息（请输入法人姓名、身份证） | 查询、刷新、导出记录 |

## 用例列表

### 一、进入个体工商户系统

#### IB-001 正常登录后进入个体工商户首页

- 测试步骤：输入手机号 `13600000000`，获取短信验证码，输入验证码 `0000` 完成登录，切换到 `个体工商户` 业务线
- 预期结果：URL 包含 `/light-accounting/home`

#### IB-002 商户财务管理菜单展示正常

- 测试步骤：进入个体工商户系统后查看左侧菜单
- 预期结果：可看到 `商户财务管理` 菜单

#### IB-003 商户财务管理子菜单完整显示

- 测试步骤：展开 `商户财务管理`
- 预期结果：可见 `充值审核`、`充值审核记录`、`商户入金记录`、`商户出金记录`、`提现审核`、`提现记录`

### 二、商户财务管理查询用例

| ID | 菜单 | 场景 | 测试步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| IBQ-001 | 充值审核 | 默认查询 | 进入 `充值审核` 页面，不填写任何条件，点击 `查询` | 列表正常加载默认数据，页面无报错 |
| IBQ-002 | 充值审核 | 日期范围查询 | 选择 `申请时间（开始日期 ~ 结束日期）` 对应的起止范围，点击 `查询` | 列表仅显示时间范围内的记录 |
| IBQ-003 | 充值审核 | 文本条件查询 | 在 `查询信息（输入订单号查询）` 对应的输入框填写有效关键字，点击 `查询` | 列表返回匹配文本条件的记录 |
| IBQ-004 | 充值审核 | 下拉条件查询 | 分别选择 `所属机构`、`主体` 或 `收费类型` 中的任一选项，点击 `查询` | 列表仅显示符合所选下拉条件的记录 |
| IBQ-005 | 充值审核 | 组合条件查询 | 组合设置 `申请时间（开始日期 ~ 结束日期）`、`所属机构`、`查询信息（输入订单号查询）` 以及 `个体户名称（请输入个体户名称）`，点击 `查询` | 列表返回同时满足多个查询条件的记录 |
| IBQ-006 | 充值审核 | 无结果查询 | 在文本输入框中输入不存在的条件，点击 `查询` | 列表为空或显示无数据状态 |
| IBQ-007 | 充值审核 | 刷新恢复默认 | 先执行一次有条件查询，再点击 `刷新` | 页面查询条件恢复默认，列表恢复默认查询结果 |
| IBQ-008 | 充值审核记录 | 默认查询 | 进入 `充值审核记录` 页面，不填写任何条件，点击 `查询` | 列表正常加载默认数据，页面无报错 |
| IBQ-009 | 充值审核记录 | 日期范围查询 | 选择 `申请时间（开始日期 ~ 结束日期）` 对应的起止范围，点击 `查询` | 列表仅显示时间范围内的记录 |
| IBQ-010 | 充值审核记录 | 文本条件查询 | 在 `支付渠道` 对应的输入框填写有效关键字，点击 `查询` | 列表返回匹配文本条件的记录 |
| IBQ-011 | 充值审核记录 | 下拉条件查询 | 分别选择 `所属机构`、`主体` 或 `查询信息（输入订单号查询）` 中的任一选项，点击 `查询` | 列表仅显示符合所选下拉条件的记录 |
| IBQ-012 | 充值审核记录 | 组合条件查询 | 组合设置 `申请时间（开始日期 ~ 结束日期）`、`所属机构`、`支付渠道` 以及 `个体户名称（请输入个体户名称）`，点击 `查询` | 列表返回同时满足多个查询条件的记录 |
| IBQ-013 | 充值审核记录 | 无结果查询 | 在文本输入框中输入不存在的条件，点击 `查询` | 列表为空或显示无数据状态 |
| IBQ-014 | 充值审核记录 | 刷新恢复默认 | 先执行一次有条件查询，再点击 `刷新` | 页面查询条件恢复默认，列表恢复默认查询结果 |
| IBQ-015 | 商户入金记录 | 默认查询 | 进入 `商户入金记录` 页面，不填写任何条件，点击 `查询` | 列表正常加载默认数据，页面无报错 |
| IBQ-016 | 商户入金记录 | 日期范围查询 | 选择 `交易时间（开始日期 ~ 结束日期）` 对应的起止范围，点击 `查询` | 列表仅显示时间范围内的记录 |
| IBQ-017 | 商户入金记录 | 文本条件查询 | 在 `支付渠道` 对应的输入框填写有效关键字，点击 `查询` | 列表返回匹配文本条件的记录 |
| IBQ-018 | 商户入金记录 | 下拉条件查询 | 分别选择 `所属机构`、`主体` 或 `平台订单号（请输入平台订单号）` 中的任一选项，点击 `查询` | 列表仅显示符合所选下拉条件的记录 |
| IBQ-019 | 商户入金记录 | 组合条件查询 | 组合设置 `交易时间（开始日期 ~ 结束日期）`、`所属机构`、`支付渠道` 以及 `是否合并账户`，点击 `查询` | 列表返回同时满足多个查询条件的记录 |
| IBQ-020 | 商户入金记录 | 无结果查询 | 在文本输入框中输入不存在的条件，点击 `查询` | 列表为空或显示无数据状态 |
| IBQ-021 | 商户入金记录 | 刷新恢复默认 | 先执行一次有条件查询，再点击 `刷新` | 页面查询条件恢复默认，列表恢复默认查询结果 |
| IBQ-022 | 商户出金记录 | 默认查询 | 进入 `商户出金记录` 页面，不填写任何条件，点击 `查询` | 列表正常加载默认数据，页面无报错 |
| IBQ-023 | 商户出金记录 | 日期范围查询 | 选择 `交易时间（开始日期 ~ 结束日期）` 对应的起止范围，点击 `查询` | 列表仅显示时间范围内的记录 |
| IBQ-024 | 商户出金记录 | 文本条件查询 | 在 `支付渠道` 对应的输入框填写有效关键字，点击 `查询` | 列表返回匹配文本条件的记录 |
| IBQ-025 | 商户出金记录 | 下拉条件查询 | 分别选择 `所属机构`、`主体` 或 `平台订单号（请输入平台订单号）` 中的任一选项，点击 `查询` | 列表仅显示符合所选下拉条件的记录 |
| IBQ-026 | 商户出金记录 | 组合条件查询 | 组合设置 `交易时间（开始日期 ~ 结束日期）`、`所属机构`、`支付渠道` 以及 `是否合并账户`，点击 `查询` | 列表返回同时满足多个查询条件的记录 |
| IBQ-027 | 商户出金记录 | 无结果查询 | 在文本输入框中输入不存在的条件，点击 `查询` | 列表为空或显示无数据状态 |
| IBQ-028 | 商户出金记录 | 刷新恢复默认 | 先执行一次有条件查询，再点击 `刷新` | 页面查询条件恢复默认，列表恢复默认查询结果 |
| IBQ-029 | 提现审核 | 默认查询 | 进入 `提现审核` 页面，不填写任何条件，点击 `查询` | 列表正常加载默认数据，页面无报错 |
| IBQ-030 | 提现审核 | 日期范围查询 | 选择 `申请时间（开始日期 ~ 结束日期）` 对应的起止范围，点击 `查询` | 列表仅显示时间范围内的记录 |
| IBQ-031 | 提现审核 | 文本条件查询 | 在 `订单号（请输入订单号）` 对应的输入框填写有效关键字，点击 `查询` | 列表返回匹配文本条件的记录 |
| IBQ-032 | 提现审核 | 下拉条件查询 | 分别选择 `所属机构`、`主体` 或 `模式` 中的任一选项，点击 `查询` | 列表仅显示符合所选下拉条件的记录 |
| IBQ-033 | 提现审核 | 组合条件查询 | 组合设置 `申请时间（开始日期 ~ 结束日期）`、`所属机构`、`订单号（请输入订单号）` 以及 `个体户法人信息（请输入法人姓名、身份证）`，点击 `查询` | 列表返回同时满足多个查询条件的记录 |
| IBQ-034 | 提现审核 | 无结果查询 | 在文本输入框中输入不存在的条件，点击 `查询` | 列表为空或显示无数据状态 |
| IBQ-035 | 提现审核 | 刷新恢复默认 | 先执行一次有条件查询，再点击 `刷新` | 页面查询条件恢复默认，列表恢复默认查询结果 |
| IBQ-036 | 提现记录 | 默认查询 | 进入 `提现记录` 页面，不填写任何条件，点击 `查询` | 列表正常加载默认数据，页面无报错 |
| IBQ-037 | 提现记录 | 日期范围查询 | 选择 `申请时间（开始日期 ~ 结束日期）` 对应的起止范围，点击 `查询` | 列表仅显示时间范围内的记录 |
| IBQ-038 | 提现记录 | 文本条件查询 | 在 `订单号（请输入订单号）` 对应的输入框填写有效关键字，点击 `查询` | 列表返回匹配文本条件的记录 |
| IBQ-039 | 提现记录 | 下拉条件查询 | 分别选择 `所属机构`、`主体` 或 `模式` 中的任一选项，点击 `查询` | 列表仅显示符合所选下拉条件的记录 |
| IBQ-040 | 提现记录 | 组合条件查询 | 组合设置 `申请时间（开始日期 ~ 结束日期）`、`所属机构`、`订单号（请输入订单号）` 以及 `个体户法人信息（请输入法人姓名、身份证）`，点击 `查询` | 列表返回同时满足多个查询条件的记录 |
| IBQ-041 | 提现记录 | 无结果查询 | 在文本输入框中输入不存在的条件，点击 `查询` | 列表为空或显示无数据状态 |
| IBQ-042 | 提现记录 | 刷新恢复默认 | 先执行一次有条件查询，再点击 `刷新` | 页面查询条件恢复默认，列表恢复默认查询结果 |

## 自动化脚本对应关系

- 当前脚本：`playwright_individual_business_test.py`
- 已覆盖场景：`IB-001`、`IB-002`、`IB-003`
- 待扩展自动化场景：`IBQ-001` 至 `IBQ-042`

## 备注

- 本文档查询项与按钮基于实际页面采集结果整理
- 充值审核页面还存在 `企业支付宝充值`、`连连网银充值` 等功能按钮，后续可单独设计功能用例
